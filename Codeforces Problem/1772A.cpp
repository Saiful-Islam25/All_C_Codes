#include <bits/stdc++.h>
using namespace std;
int main()
{   ios_base:: sync_with_stdio(false);
    cin.tie(NULL);
    int t, res;
    cin >> t;

    while (t--)
    {
        int a, b;
        cin >> a >> b;
        res = a + b;
        cout << res << "\n";
    }
    return 0;
}